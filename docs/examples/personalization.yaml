# Copy the sections you want into the top level of config.yaml, then remove
# the leading "# " markers. Every line remains commented here so this template
# is inert if it is copied wholesale.

# mcp:
#   enabled: true
#   servers:
#     filesystem:
#       command: npx
#       args:
#         - -y
#         - "@modelcontextprotocol/server-filesystem"
#         - /absolute/path/to/allowed/files
#       enabled: true
#       tool_allow:
#         - read_file
#         - list_directory
#       tool_deny: []
#       timeout_seconds: 30
#
#     remote-example:
#       url: http://example-host:8080/mcp
#       headers:
#         Authorization: "Bearer ${MY_MCP_TOKEN}"
#       enabled: false
#       tool_allow: []
#       tool_deny: []
#       timeout_seconds: 30

# skills:
#   enabled: true
#   dirs:
#     - ~/my-agent-skills
#   disabled: []
